import SwiftUI

struct PracticeView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Practice",
            systemImage: "pencil.and.scribble",
            message: "Handwriting tracing practice, built left-handed from the ground up, will live here."
        )
    }
}

#Preview {
    PracticeView()
}
